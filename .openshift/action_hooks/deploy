DEPLOY_DIR=$OPENSHIFT_PRIMARY_CARTRIDGE_DIR/webapps/
#delete the old deployment
rm ${DEPLOY_DIR}/ROOT.war
cd ${DEPLOY_DIR}
#download  and extract the latest product
curl -u$JENKINS_USERNAME:$JENKINS_PASSWORD  -ov "$JENKINS_URLjob/braces-build/lastSuccessfulBuild/artifact/braces-web/target/ROOT.war"